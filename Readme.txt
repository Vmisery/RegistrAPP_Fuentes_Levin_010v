Para utilizar la app:

Instalar IONIC Framework

En CMD: cd /ruta_de_registrapp/
- ionic serve (Debería compilar la APP)

Luego abrir otro CMD:

Instalar Json-server

cd /ruta_de_registrapp/registrapp/src/app/data

-json-server --watch data.json --port 3300 (visualizar archivo json y definir puerto 3300)